$kind: http-request
name: GET /Organization/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Organization/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
