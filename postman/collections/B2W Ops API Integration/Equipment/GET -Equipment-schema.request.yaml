$kind: http-request
name: GET /Equipment/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Equipment/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
