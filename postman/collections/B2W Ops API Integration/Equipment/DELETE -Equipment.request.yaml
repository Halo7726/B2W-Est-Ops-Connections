$kind: http-request
name: DELETE /Equipment
description: Delete an entity.
url: "{{BaseURL}}/Equipment"
method: DELETE
headers:
  - key: ConnectionString
    value: "{{ConnectionString}}"
  - key: ObjectID
    value: "{{ObjectID}}"
    disabled: true
queryParams:
  - key: ObjectID
    value: "{{ObjectID}}"
    disabled: true
    description: PK of the entity to delete
order: 4000
