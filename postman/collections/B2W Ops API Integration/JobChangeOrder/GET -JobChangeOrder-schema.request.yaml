$kind: http-request
name: GET /JobChangeOrder/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobChangeOrder/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
