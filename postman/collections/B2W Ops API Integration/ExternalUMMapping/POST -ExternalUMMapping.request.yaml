$kind: http-request
name: POST /ExternalUMMapping
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/ExternalUMMapping"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
