$kind: http-request
name: GET /JobFileAttachment/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobFileAttachment/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
