$kind: http-request
name: GET /Subcategory/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Subcategory/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
