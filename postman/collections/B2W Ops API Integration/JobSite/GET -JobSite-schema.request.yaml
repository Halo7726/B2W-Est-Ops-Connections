$kind: http-request
name: GET /JobSite/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobSite/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
