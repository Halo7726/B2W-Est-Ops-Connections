$kind: http-request
name: GET /JobSubcontractorQuote/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobSubcontractorQuote/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
