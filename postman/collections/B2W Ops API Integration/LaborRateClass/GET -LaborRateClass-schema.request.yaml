$kind: http-request
name: GET /LaborRateClass/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/LaborRateClass/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
