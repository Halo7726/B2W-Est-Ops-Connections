$kind: http-request
name: POST /LaborRateClass
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/LaborRateClass"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
