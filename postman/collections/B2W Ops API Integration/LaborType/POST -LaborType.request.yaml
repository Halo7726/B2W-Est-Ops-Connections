$kind: http-request
name: POST /LaborType
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/LaborType"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
