$kind: http-request
name: POST /ProductionAccount
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/ProductionAccount"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
