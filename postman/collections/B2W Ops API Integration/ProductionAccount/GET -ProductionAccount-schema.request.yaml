$kind: http-request
name: GET /ProductionAccount/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/ProductionAccount/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
