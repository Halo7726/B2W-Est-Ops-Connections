$kind: http-request
name: POST /JobEstimateItem
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/JobEstimateItem"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
