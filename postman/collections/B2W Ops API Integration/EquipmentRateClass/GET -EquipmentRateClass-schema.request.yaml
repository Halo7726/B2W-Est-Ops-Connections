$kind: http-request
name: GET /EquipmentRateClass/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/EquipmentRateClass/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
