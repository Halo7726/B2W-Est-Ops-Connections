$kind: http-request
name: POST /EquipmentRateClass
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/EquipmentRateClass"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
