$kind: http-request
name: GET /SystemInfo
description: Returns information around the related Ops system configuration
url: "{{BaseURL}}/SystemInfo"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 1000
