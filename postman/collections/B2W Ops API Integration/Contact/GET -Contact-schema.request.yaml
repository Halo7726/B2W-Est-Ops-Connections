$kind: http-request
name: GET /Contact/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Contact/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
