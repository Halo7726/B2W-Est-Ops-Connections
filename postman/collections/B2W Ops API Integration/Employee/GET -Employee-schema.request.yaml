$kind: http-request
name: GET /Employee/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Employee/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
