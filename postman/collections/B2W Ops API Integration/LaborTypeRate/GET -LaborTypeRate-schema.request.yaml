$kind: http-request
name: GET /LaborTypeRate/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/LaborTypeRate/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
