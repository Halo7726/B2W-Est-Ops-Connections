$kind: http-request
name: GET /UnitOfMeasure/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/UnitOfMeasure/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 2000
