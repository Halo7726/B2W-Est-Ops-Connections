$kind: http-request
name: GET /TransportCrewTemplate/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/TransportCrewTemplate/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 2000
