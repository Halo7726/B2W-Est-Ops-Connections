$kind: http-request
name: GET /JobOverheadAccount/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobOverheadAccount/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
