$kind: http-request
name: POST /JobOverheadAccount
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/JobOverheadAccount"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
