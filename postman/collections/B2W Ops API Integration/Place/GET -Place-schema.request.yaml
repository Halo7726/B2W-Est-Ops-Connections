$kind: http-request
name: GET /Place/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Place/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
