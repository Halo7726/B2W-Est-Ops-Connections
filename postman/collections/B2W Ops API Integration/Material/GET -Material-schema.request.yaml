$kind: http-request
name: GET /Material/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Material/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
