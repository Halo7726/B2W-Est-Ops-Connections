$kind: http-request
name: GET /PartInventory/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/PartInventory/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
