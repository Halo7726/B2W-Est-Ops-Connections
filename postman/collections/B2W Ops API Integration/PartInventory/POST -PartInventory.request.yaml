$kind: http-request
name: POST /PartInventory
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/PartInventory"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
