$kind: http-request
name: POST /JobProjectManager
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/JobProjectManager"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
