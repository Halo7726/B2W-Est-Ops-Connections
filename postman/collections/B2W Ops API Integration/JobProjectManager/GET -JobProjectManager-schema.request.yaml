$kind: http-request
name: GET /JobProjectManager/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobProjectManager/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 4000
