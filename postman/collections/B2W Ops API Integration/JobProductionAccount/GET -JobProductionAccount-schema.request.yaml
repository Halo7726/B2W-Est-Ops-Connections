$kind: http-request
name: GET /JobProductionAccount/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobProductionAccount/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
