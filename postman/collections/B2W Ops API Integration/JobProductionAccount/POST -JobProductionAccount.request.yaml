$kind: http-request
name: POST /JobProductionAccount
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/JobProductionAccount"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
