$kind: http-request
name: GET /Factor/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Factor/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
