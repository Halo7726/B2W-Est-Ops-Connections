$kind: http-request
name: POST /JobCostBreakdownElement
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/JobCostBreakdownElement"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
