$kind: http-request
name: GET /JobCostBreakdownElement/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobCostBreakdownElement/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
