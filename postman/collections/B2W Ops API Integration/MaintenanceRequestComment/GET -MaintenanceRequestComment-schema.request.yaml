$kind: http-request
name: GET /MaintenanceRequestComment/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/MaintenanceRequestComment/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
