$kind: http-request
name: POST /ResourceEvent
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/ResourceEvent"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
