$kind: http-request
name: GET /SubcontractedTruck/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/SubcontractedTruck/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
