$kind: http-request
name: POST /OverheadAccount
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/OverheadAccount"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
