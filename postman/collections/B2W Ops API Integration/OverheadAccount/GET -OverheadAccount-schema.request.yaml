$kind: http-request
name: GET /OverheadAccount/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/OverheadAccount/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
