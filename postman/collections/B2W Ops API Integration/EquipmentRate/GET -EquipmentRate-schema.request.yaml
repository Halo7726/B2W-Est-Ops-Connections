$kind: http-request
name: GET /EquipmentRate/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/EquipmentRate/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
