$kind: http-request
name: POST /EquipmentRate
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/EquipmentRate"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
