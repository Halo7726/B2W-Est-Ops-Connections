$kind: http-request
name: POST /TrackWorkOrder
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/TrackWorkOrder"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
