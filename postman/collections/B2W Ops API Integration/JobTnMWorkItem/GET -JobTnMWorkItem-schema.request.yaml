$kind: http-request
name: GET /JobTnMWorkItem/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobTnMWorkItem/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
