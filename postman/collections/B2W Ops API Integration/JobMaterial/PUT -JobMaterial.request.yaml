$kind: http-request
name: PUT /JobMaterial
description: Update an entity based on the specified model. First get the entity
  (based on its key), update relevant fields, and then PUT it.
url: "{{BaseURL}}/JobMaterial"
method: PUT
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 3000
