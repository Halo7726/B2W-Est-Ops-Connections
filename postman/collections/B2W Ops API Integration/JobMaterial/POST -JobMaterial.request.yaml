$kind: http-request
name: POST /JobMaterial
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/JobMaterial"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
