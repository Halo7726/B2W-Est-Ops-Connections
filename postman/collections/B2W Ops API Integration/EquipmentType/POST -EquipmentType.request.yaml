$kind: http-request
name: POST /EquipmentType
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/EquipmentType"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
