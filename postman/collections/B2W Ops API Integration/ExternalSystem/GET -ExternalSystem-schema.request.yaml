$kind: http-request
name: GET /ExternalSystem/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/ExternalSystem/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
