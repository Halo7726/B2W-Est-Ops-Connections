$kind: http-request
name: POST /ExternalSystem
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/ExternalSystem"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
