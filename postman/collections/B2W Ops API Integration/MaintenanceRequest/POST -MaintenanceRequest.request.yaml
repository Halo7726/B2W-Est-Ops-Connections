$kind: http-request
name: POST /MaintenanceRequest
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/MaintenanceRequest"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 3000
