$kind: http-request
name: GET /Category/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/Category/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
