$kind: http-request
name: POST /JobProductionTarget
description: Create a new entity based on the specified model.
url: "{{BaseURL}}/JobProductionTarget"
method: POST
headers:
  ConnectionString: "{{ConnectionString}}"
body:
  type: json
  content: ""
order: 2000
