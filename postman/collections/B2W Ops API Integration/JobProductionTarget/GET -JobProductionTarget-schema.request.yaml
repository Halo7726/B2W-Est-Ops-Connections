$kind: http-request
name: GET /JobProductionTarget/schema
description: Get the JSON schema associated with a model.
url: "{{BaseURL}}/JobProductionTarget/schema"
method: GET
headers:
  ConnectionString: "{{ConnectionString}}"
order: 5000
